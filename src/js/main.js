// This file serves as the entry point for the JavaScript code. 
// It initializes the application, sets up event listeners, 
// and manages the overall flow of the interactive story.

document.addEventListener('DOMContentLoaded', () => {
    loadChapters();
    setupNavigation();
});